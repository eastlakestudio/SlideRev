# SlideRev 综合修复与优化任务 (v12)

- [x] 修复 PPTXExporter.swift 中的 XML 语法错误
- [x] 修复手动 Refine 同步 Bug (AdvancedSlideProcessor.swift)
- [x] 核心功能交互逻辑对齐 (PRD 规范)
    - [x] 更新 `prd.md` 定义
    - [x] 修复 `AdvancedSlideProcessor.swift` 中的 `processPage` 缓存拦截 Bug (Reset 快速化重构)
    - [x] 在 `refreshInpaintedBackgroundSilently` 中补全同步逻辑
- [x] UI 界面与交互调整
    - [x] 将主窗口最小宽度锁定为 1200
    - [x] 水印自定义输入框宽度增加 20% (由 180 增至 220)
    - [x] 再次加宽 20% (由 220 增至 264)
- [x] 批量重构深度优化
    - [x] 修复缓存导致批量跳过的 Bug
    - [x] 实现“全页精炼”逻辑 (Erase All -> Restore based on Pattern)
    - [x] 增加批量重构扫描动效与详细进度文本
- [x] 最终构建与全量验证
    - [x] 运行 `./build_native.sh`
    - [x] 验证 Reset 是否能秒级清空状态并保留 OCR
    - [x] 验证深层 Batch Refine 后的状态持久化与视觉效果
- [x] 文档整理
    - [x] 创建 `docs/dev_process/` 目录并将核心文档存入
